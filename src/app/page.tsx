// import Image from "next/image";
// "use client";

import Header from "./components/templates/header";

const Home = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between bg-primary-dark">
      <Header />
    </main>
  );
};

export default Home;
